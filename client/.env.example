# ============================================
# CONFIGURACIÓN DEL SERVIDOR CENTRAL
# ============================================
# Estas configuraciones deben ser IGUALES en todas las parroquias
# Solo cambia la IP si tu servidor central tiene otra dirección

# URL del servidor CouchDB central (SIN incluir usuario/contraseña aquí)
# Ejemplo: http://192.168.1.100:5984 o http://sync.diocesis.local:5984
VITE_COUCHDB_URL=http://localhost:5984

# Nombre de la base de datos (DEBE SER IGUAL EN TODAS LAS PARROQUIAS)
VITE_COUCHDB_DB_NAME=sacramentos

# Credenciales del servidor central (IGUALES EN TODAS LAS PARROQUIAS)
VITE_COUCHDB_USERNAME=admin
VITE_COUCHDB_PASSWORD=password

# ============================================
# CONFIGURACIÓN DE ESTA PARROQUIA
# ============================================
# Estas configuraciones deben ser ÚNICAS para cada parroquia

# ID único de esta parroquia (usar formato: PARROQUIA_XXX)
VITE_PARISH_ID=PARROQUIA_001

# Nombre completo de esta parroquia (para identificación en logs)
VITE_PARISH_NAME=Parroquia San Miguel

# ============================================
# SEGURIDAD
# ============================================
# Cada parroquia debe tener su propia clave de encriptación

# Habilitar encriptación local (recomendado)
VITE_ENCRYPTION_ENABLED=true

# Clave de encriptación (DEBE SER ÚNICA POR PARROQUIA)
# Generar con: openssl rand -base64 32
VITE_ENCRYPTION_KEY=your-secure-key-here-change-in-production

# ============================================
# CONFIGURACIÓN AVANZADA
# ============================================

# Habilitar Service Worker para funcionamiento offline
VITE_ENABLE_SERVICE_WORKER=true

# Intervalo de reintento de sincronización (milisegundos)
VITE_SYNC_RETRY_INTERVAL=5000

# Número máximo de reintentos de sincronización
VITE_SYNC_MAX_RETRIES=3

# ============================================
# INSTRUCCIONES DE INSTALACIÓN
# ============================================
# 1. Copiar este archivo como .env (sin .example)
# 2. Cambiar VITE_COUCHDB_URL a la IP del servidor central
# 3. Cambiar VITE_COUCHDB_PASSWORD a la contraseña real
# 4. Cambiar VITE_PARISH_ID y VITE_PARISH_NAME para esta parroquia
# 5. Generar nueva VITE_ENCRYPTION_KEY única
# 6. Guardar y reiniciar la aplicación
